---
alwaysApply: true
---



# 🌐 C-Chat IM 系统 - 项目规范 (MDC)

## 🎯 核心设计原则
- **简洁清新**：低饱和度配色 + 柔和阴影 + 呼吸感留白
- **现代化交互**：微动效 + 智能状态反馈 + 无障碍支持
- **跨端一致性**：Electron 桌面端与移动端体验对齐
- **开发者友好**：清晰命名 + 模块化解耦 + 类型安全

---

## 🎨 配色系统 (Tailwind 配置)
```js
// tailwind.config.js
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        // 基础色系 - 简洁清新
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9', // 主色调 - 科技蓝
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
        },
        secondary: {
          500: '#8b5cf6', // 消息气泡辅助色
        },
        surface: {
          50: '#fafafa',   // 背景
          100: '#f5f5f5',  // 消息列表背景
          200: '#e5e7eb',  // 边框/分隔线
          800: '#1f2937',  // 深色模式背景
          900: '#111827',  // 深色模式深背景
        },
        status: {
          online: '#10b981',   // 在线 - 翠绿
          away: '#f59e0b',     // 离开 - 暖黄
          busy: '#ef4444',     // 忙碌 - 珊瑚红
          offline: '#9ca3af',  // 离线 - 灰
        },
        // 语义色
        success: '#10b981',
        warning: '#f59e0b',
        error: '#ef4444',
        info: '#3b82f6',
      },
      borderRadius: {
        'xl': '16px',    // 消息气泡圆角
        '2xl': '24px',   // 卡片圆角
        '3xl': '32px',   // 头像圆角
      },
      animation: {
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
        'fade-in': 'fadeIn 0.3s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: 0, transform: 'translateY(4px)' },
          '100%': { opacity: 1, transform: 'translateY(0)' },
        }
      }
    }
  },
  plugins: [require('@tailwindcss/forms')],
}