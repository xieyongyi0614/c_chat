export * as store from './storeTable';

export * as tableNames from './tableNames';

/** 通用的配置，不区分窗口的配置 */
export const GLOBAL_WINDOW_ID = 0;
// TODO 目前仅支持单窗口，后续支持多窗口
export const DEFAULT_WINDOW_ID = 1;
